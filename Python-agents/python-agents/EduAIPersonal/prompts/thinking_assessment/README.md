# Thinking assessment prompts

Store prompt templates used to assess a learner's thinking here. Each template must produce an AI draft only; a
teacher reviews and finalizes any recorded score or feedback.

Use `rubric.md` as the default rubric until a course-specific rubric is added.
