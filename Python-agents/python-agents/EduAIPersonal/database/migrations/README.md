Add ordered, forward-only SQLite migrations here. `init.sql` is the fresh-database baseline; do not edit historical migrations after they are applied.

For an existing database created before `teacher_configurations`, apply `001_add_teacher_configurations.sql` once.
